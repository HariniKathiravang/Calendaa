{
  "framework": null,
  "buildCommand": "cd Frontend && npm install && npm run build && rm -rf ../public && mkdir -p ../public && cp -R .output/public/. ../public/",
  "outputDirectory": "public",
  "functions": {
    "api/index.py": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index.py" },
    { "source": "/:path*",     "destination": "/" }
  ]
}
