{
  "buildCommand": "npm run build && rm -rf public && mkdir -p public && cp -R .output/public/. public/",
  "installCommand": "npm install",
  "outputDirectory": "public",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
